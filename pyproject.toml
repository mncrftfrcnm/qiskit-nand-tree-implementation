[build-system]
requires = ["setuptools>=77.0.3", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "qiskit-nand-tree-implementation"
version = "0.6.2"
description = "Finite quantum NAND-tree walk experiments with Qiskit"
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [
  { name = "mncrftfrcnm" },
]
keywords = [
  "qiskit",
  "quantum-computing",
  "nand-tree",
  "quantum-walk",
  "hamiltonian-simulation",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Scientific/Engineering",
]

dependencies = [
  "numpy>=2.0,<3",
  "scipy>=1.14",
  "qiskit>=2.4,<3",
]

[project.optional-dependencies]
dev = [
  "build>=1.2",
  "matplotlib>=3.9",
  "pytest>=8",
  "ruff>=0.9",
]
plot = [
  "matplotlib>=3.9",
]

[project.urls]
Homepage = "https://github.com/mncrftfrcnm/qiskit-nand-tree-implementation"
Repository = "https://github.com/mncrftfrcnm/qiskit-nand-tree-implementation"
Issues = "https://github.com/mncrftfrcnm/qiskit-nand-tree-implementation/issues"

[project.scripts]
nandtree = "qiskit_implementation.cli:main"

[tool.setuptools]
py-modules = ["main", "example_usage"]

[tool.setuptools.packages.find]
where = ["."]
include = [
  "qiskit_implementation*",
  "non_qiskit*",
]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
markers = [
  "slow: exhaustive Qiskit checks",
]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
